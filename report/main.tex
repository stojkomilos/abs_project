\documentclass[12pt,a4paper]{article}

% ---------------------------------------------------------------------------
% Kodiranje, jezik, fontovi
% ---------------------------------------------------------------------------
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{lmodern}
% Napomena: babel sa srpskim (serbian.ldf) nije dostupan u svim TeX
% distribucijama, pa su nazivi (Slika, Tabela, Sadržaj...) postavljeni
% ručno radi prenosivosti. Na Overleaf-u se umesto ovoga može koristiti
% \usepackage[serbian]{babel}.
\renewcommand{\figurename}{Slika}
\renewcommand{\tablename}{Tabela}
\renewcommand{\contentsname}{Sadržaj}
\renewcommand{\refname}{Literatura}
\renewcommand{\abstractname}{Sažetak}

% ---------------------------------------------------------------------------
% Geometrija i tipografija
% ---------------------------------------------------------------------------
\usepackage[a4paper,top=2.5cm,bottom=2.5cm,left=3cm,right=2.5cm]{geometry}
\usepackage{setspace}
\onehalfspacing
\usepackage{indentfirst}
\usepackage{parskip}

% ---------------------------------------------------------------------------
% Grafika, tabele, matematika
% ---------------------------------------------------------------------------
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{caption}
\captionsetup{font=small,labelfont=bf}
\usepackage{xcolor}
\usepackage{float}

% ---------------------------------------------------------------------------
% TikZ za blok-dijagrame
% ---------------------------------------------------------------------------
\usepackage{tikz}
\usetikzlibrary{shapes.geometric,arrows.meta,positioning,calc}

% ---------------------------------------------------------------------------
% Zaglavlja i numeracija
% ---------------------------------------------------------------------------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small Analiza biomedicinske slike}
\fancyhead[R]{\small Klasifikacija MRI snimaka lumbalne kičme}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}

\usepackage[hidelinks]{hyperref}
\usepackage{url}

% ---------------------------------------------------------------------------
% Pomoćna komanda: okvir kao rezervisano mesto za sliku ("placeholder")
% ---------------------------------------------------------------------------
\newcommand{\placeholderbox}[2]{%
  \begin{tikzpicture}
    \draw[dashed,thick,gray] (0,0) rectangle (\linewidth,#1);
    \node[align=center,text width=0.85\linewidth,gray] at (0.5\linewidth,0.5*#1)
      {\textbf{[MESTO ZA SLIKU]}\\[4pt]#2};
  \end{tikzpicture}%
}

% Boje za blok-dijagram
\definecolor{blokplava}{RGB}{220,235,250}
\definecolor{blokzelena}{RGB}{223,245,223}
\definecolor{bloknarandzasta}{RGB}{252,236,214}

\begin{document}

% ===========================================================================
% NASLOVNA STRANA
% ===========================================================================
\begin{titlepage}
\centering
{\large UNIVERZITET U BEOGRADU\par}
{\large ELEKTROTEHNIČKI FAKULTET\par}
\vspace{0.4cm}
{\normalsize Katedra za signale i sisteme\par}
\vspace{2.5cm}

{\normalsize Projekat iz predmeta\par}
{\large\bfseries Analiza biomedicinske slike\par}
\vspace{2.0cm}

\rule{\linewidth}{0.5pt}\\[0.5cm]
{\LARGE\bfseries Klasifikacija degenerativnih promena\\[0.2cm]
lumbalne kičme na MRI snimcima\\[0.2cm]
primenom konvolucionih neuronskih mreža\par}
\vspace{0.4cm}
\rule{\linewidth}{0.5pt}\\[0.6cm]
{\large\itshape Poređenje konvolucione mreže (ResNet-50) i vizuelnog\\
transformera (ViT) uz vizuelno objašnjenje pomoću Grad-CAM metode\par}

\vfill

\begin{minipage}{0.45\textwidth}
\begin{flushleft}
\large
\textbf{Studenti:}\\
Miloš Stojko\\
Ognjen Banjac
\end{flushleft}
\end{minipage}
\hfill
\begin{minipage}{0.45\textwidth}
\begin{flushright}
\large
\textbf{Mentor:}\\
\mbox{}[Ime mentora]
\end{flushright}
\end{minipage}

\vspace{1.5cm}
{\normalsize Beograd, jun 2026.\par}
\end{titlepage}

% ===========================================================================
% SAŽETAK
% ===========================================================================
\thispagestyle{fancy}
\section*{Sažetak}
\addcontentsline{toc}{section}{Sažetak}

Hernija lumbalnog diska je čest uzrok bola u donjem delu leđa, a njena dijagnostika
se po pravilu zasniva na pregledu snimaka magnetne rezonance (engl.\ \textit{Magnetic
Resonance Imaging}, MRI) od strane iskusnog radiologa. Zbog visoke cene pregleda i
neravnomerne raspoređenosti zdravstvenih resursa, sistemi koji automatski analiziraju
MRI snimke lumbalne kičme postaju sve značajniji. U ovom radu predstavljen je sistem
za automatsku klasifikaciju aksijalnih MRI preseka lumbalne kičme u dve kategorije:
\textit{hernija diska} i \textit{uredan nalaz}. Upoređene su dve arhitekture:
konvoluciona neuronska mreža ResNet-50, trenirana od nule, i vizuelni transformer
(\textit{Vision Transformer}, ViT-Base/16), fino podešen polazeći od težina
predtreniranih na skupu ImageNet-21k. Za vizuelnu interpretaciju odluka konvolucionog
modela korišćena je Grad-CAM metoda, koja ističe regione snimka koji su najviše uticali
na predikciju. Modeli su trenirani i evaluirani na javno dostupnom skupu podataka sa
platforme Kaggle, uz augmentaciju i regularizaciju. Najbolji model na test skupu postiže
tačnost od [XX{,}X\%], senzitivnost od [XX{,}X\%] i specifičnost od [XX{,}X\%], što je
uporedivo sa rezultatima iz relevantne literature. Grad-CAM mape potvrđuju da se
konvolucioni model pretežno fokusira na region
intervertebralnog diska i duralne vreće, što je u skladu sa kliničkim kriterijumima
dijagnostike.

\vspace{0.3cm}
\noindent\textbf{Ključne reči:} hernija lumbalnog diska, MRI, duboko učenje,
konvolucione neuronske mreže, ResNet, vizuelni transformer (ViT), Grad-CAM,
računarski potpomognuta dijagnostika.

\newpage
\tableofcontents
\newpage

% ===========================================================================
% 1. UVOD
% ===========================================================================
\section{Uvod}
\label{sec:uvod}

Intervertebralni disk se sastoji od želatinoznog jezgra (\textit{nucleus pulposus}) i
fibroznog prstena (\textit{annulus fibrosus}) koji jezgro zadržava unutar granica
pršljena. Usled povrede, dugotrajnog opterećenja ili dehidratacije vezane za starenje,
disk može da prodre van svojih anatomskih granica i da pritisne nervne strukture u
spinalnom kanalu, što dovodi do bola u leđima, bola u nozi, utrnulosti i drugih
simptoma~\cite{hou2023mri}. Ovakvo stanje se klinički dijagnostikuje kao hernija
lumbalnog diska i predstavlja jedan od najčešćih uzroka hroničnog bola u leđima.

Zlatni standard u dijagnostici je pregled MRI snimaka od strane radiologa, pri čemu se
istovremeno posmatraju sagitalni i aksijalni (transverzalni) preseci, najčešće
T2-otežani snimci~\cite{hou2023mri}. Međutim, manuelna interpretacija je vremenski
zahtevna, podložna subjektivnosti posmatrača i ograničena dostupnošću stručnjaka.
Dodatno, poznato je da se degenerativne promene diska viđaju i kod osoba bez ikakvih
simptoma~\cite{jensen1994mri}, što interpretaciju dodatno otežava. Zbog svega navedenog,
postoji jasna motivacija za razvojem sistema za računarski potpomognutu dijagnostiku
(engl.\ \textit{Computer-Aided Diagnosis}, CAD).

\paragraph{Pristupi analizi.}
Metode automatske analize MRI snimaka lumbalne kičme mogu se grubo podeliti u nekoliko
grupa: (i) klasične metode obrade slike i mašinskog učenja sa ručno projektovanim
obeležjima (segmentacija diska, merenje geometrijskih parametara); (ii) metode detekcije
i segmentacije objekata zasnovane na dubokom učenju, koje lokalizuju i označavaju tkiva
različite morfologije~\cite{litjens2017survey}; i (iii) metode klasifikacije celokupnog
snimka pomoću konvolucionih neuronskih mreža, koje direktno mapiraju snimak u
dijagnostičku kategoriju. Poslednja grupa je posebno privlačna jer ne zahteva skupo
piksel-precizno označavanje, već samo oznaku na nivou snimka.

\paragraph{Pregled literature.}
Duboko učenje je poslednjih godina postalo dominantan pristup u analizi medicinskih
slika~\cite{litjens2017survey}. U domenu lumbalne kičme, veliki broj radova oslanja se
isključivo na centralni sagitalni presek ili na paradigmu detekcije objekata, čime se
može izgubiti deo informacija sadržanih u aksijalnim presecima. Hou i saradnici~\cite{hou2023mri}
predlažu model za automatsku dijagnostiku hernije lumbalnog diska zasnovan na
\textit{polu-nadgledanom} učenju (engl.\ \textit{semi-supervised learning}). Njihov
pristup koristi kontrastivno samonadgledano predtreniranje na neoznačenim MRI snimcima
kako bi se dobio ekstraktor obeležja, a zatim se aksijalni preseci (centralni presek
diska i njemu susedni slojevi) propuštaju kroz taj ekstraktor i klasifikuju pomoću
višeslojnog perceptrona. Na test skupu postižu tačnost od $87{,}11\%$, senzitivnost od
$87{,}50\%$, specifičnost od $86{,}72\%$ i AUC od $0{,}9487$, uz heatmap prikaz težine
degenerativnih promena radi lakše interpretacije rezultata. Ovaj rad predstavlja glavnu
referencu na koju se oslanjamo, kako po izboru ulaznih podataka (aksijalni preseci) tako
i po ideji vizuelnog objašnjenja predikcije.

Sa metodološke strane, oslanjamo se na rezidualne mreže (ResNet)~\cite{he2016deep}, koje
su uvođenjem rezidualnih veza omogućile stabilno treniranje vrlo dubokih mreža, na
vizuelne transformere (ViT)~\cite{dosovitskiy2021vit}, koji su pokazali da se
arhitektura zasnovana na pažnji (\textit{self-attention}) može uspešno primeniti na
klasifikaciju slika, kao i na Grad-CAM~\cite{selvaraju2017gradcam} metodu za generisanje
toplotnih mapa značajnosti koje objašnjavaju odluku konvolucione mreže.

\paragraph{Cilj projekta.}
Cilj ovog projekta je da se razvije i evaluira sistem za binarnu klasifikaciju aksijalnih
MRI preseka lumbalne kičme u kategorije \textit{hernija diska} i \textit{uredan nalaz}.
Pri tome se upoređuju dve reprezentativne familije arhitektura: konvoluciona neuronska
mreža ResNet-50~\cite{he2016deep}, trenirana od nule, i vizuelni transformer
ViT-Base/16~\cite{dosovitskiy2021vit}, fino podešen polazeći od težina predtreniranih na
velikom skupu ImageNet-21k. Dodatni ciljevi su da se odluke konvolucionog modela učine
interpretabilnim pomoću Grad-CAM toplotnih mapa, da se postignuti rezultati uporede sa
rezultatima iz literature~\cite{hou2023mri} i da se analiziraju tipični uspesi i promašaji
modela.

% ===========================================================================
% 2. METODA
% ===========================================================================
\section{Metoda}
\label{sec:metoda}

\subsection{Teorijske osnove}
\label{sec:teorija}

\subsubsection{Konvolucione neuronske mreže}
\label{sec:cnn}

Konvoluciona neuronska mreža (engl.\ \textit{Convolutional Neural Network}, CNN) je
arhitektura dubokog učenja specijalizovana za podatke sa mrežnom (prostornom) strukturom,
kao što su slike. Osnovni gradivni element je \textbf{konvolucioni sloj}, u kome se mali
filteri (jezgra, engl.\ \textit{kernels}) pomeraju (konvoluiraju) preko ulaza i u svakoj
poziciji računaju težinsku sumu lokalnog okruženja piksela, dajući \textit{mapu obeležja}
(engl.\ \textit{feature map}). Za ulaz $x$ i jezgro $w$ dimenzija $K\times K$, izlaz na
poziciji $(i,j)$ je
\begin{equation}
y_{ij} = \sum_{m=0}^{K-1}\sum_{n=0}^{K-1} w_{mn}\, x_{\,i+m,\,j+n} + b,
\label{eq:konv}
\end{equation}
gde je $b$ slobodni član (engl.\ \textit{bias}). Konvolucioni slojevi imaju dve ključne
osobine koje ih čine pogodnim za slike: (i) \textbf{lokalnu povezanost} --- svaki neuron
,,vidi'' samo malo receptivno polje ulaza, i (ii) \textbf{deljenje parametara} --- isti
filter primenjuje se na celu sliku, što drastično smanjuje broj parametara i obezbeđuje
\textit{translatornu ekvivarijantnost} (pomeraj na ulazu daje odgovarajući pomeraj na
izlazu). Nakon konvolucije primenjuje se nelinearna aktivaciona funkcija, najčešće
ispravljena linearna jedinica (engl.\ \textit{Rectified Linear Unit}, ReLU),
$\mathrm{ReLU}(z)=\max(0,z)$, koja uvodi nelinearnost neophodnu za učenje složenih
preslikavanja.

Smenjivanjem konvolucionih slojeva sa operacijama \textbf{sažimanja} (engl.\
\textit{pooling}) ili konvolucijama sa korakom (engl.\ \textit{stride}) prostorna
rezolucija se postepeno smanjuje, dok se receptivno polje uvećava; tako mreža gradi
\textbf{hijerarhiju obeležja} --- od jednostavnih ivica i tekstura u plitkim slojevima, do
delova objekata i složenih semantičkih struktura u dubljim slojevima. Radi stabilnijeg i
bržeg treniranja koristi se i \textit{batch} normalizacija. Na kraju mreže, globalnim
sažimanjem mapa obeležja i jednim ili više potpuno povezanih slojeva dobija se vektor
verovatnoća klasa.

\subsubsection{Rezidualne mreže (ResNet-50)}
\label{sec:resnet}

Povećavanjem dubine ,,obične'' konvolucione mreže performanse u praksi prestaju da rastu i
čak opadaju --- ne samo zbog preprilagođavanja, već i zbog otežane optimizacije i
nestajućih/eksplodirajućih gradijenata. Rezidualne mreže (ResNet)~\cite{he2016deep}
rešavaju ovaj problem uvođenjem \textbf{rezidualnih (preskočnih) veza}. Umesto da blok uči
ciljano preslikavanje $\mathcal{H}(x)$ direktno, on uči \textit{rezidual}
$\mathcal{F}(x)=\mathcal{H}(x)-x$, dok se ulaz $x$ prosleđuje preko identičke (\textit{skip})
veze, tako da blok računa
\begin{equation}
\mathcal{H}(x)=\mathcal{F}(x)+x.
\label{eq:rezidual}
\end{equation}
Učenje reziduala je lakše (u graničnom slučaju blok lako nauči identičko preslikavanje
postavljanjem $\mathcal{F}(x)\!\approx\!0$), a identička veza obezbeđuje nesmetan protok
gradijenta unazad, što omogućava stabilno treniranje mreža sa više desetina ili stotina
slojeva.

Korišćena varijanta \textbf{ResNet-50} ima 50 slojeva organizovanih u tzv.\
,,\textit{bottleneck}'' blokove, od kojih se svaki sastoji od tri konvolucije
($1\times1$, $3\times3$, $1\times1$): prva $1\times1$ konvolucija smanjuje broj kanala,
$3\times3$ obrađuje obeležja, a poslednja $1\times1$ ih vraća na veću dimenziju, čime se
smanjuje računska cena. Mreža počinje konvolucijom $7\times7$ i sažimanjem, a zatim slede
četiri grupe bottleneck blokova (sa po 3, 4, 6 i 3 bloka), nakon kojih dolaze globalno
usrednjavanje i potpuno povezan klasifikacioni sloj. ResNet-50 ima oko 25 miliona
parametara i predstavlja standardnu, dobro proučenu osnovicu za zadatke klasifikacije
slika.

\subsubsection{Vizuelni transformer (ViT)}
\label{sec:vit}

Vizuelni transformer (engl.\ \textit{Vision Transformer}, ViT)~\cite{dosovitskiy2021vit}
prenosi arhitekturu \textit{transformera}, izvorno razvijenu za obradu prirodnog jezika,
na domen slika. Ulazna slika se najpre deli na mrežu nepreklapajućih isečaka (engl.\
\textit{patches}) veličine $16\times16$ piksela; svaki isečak se ,,izravna'' i linearnom
projekcijom preslikava u vektor fiksne dimenzije (token, engl.\ \textit{patch embedding}).
Tako se dvodimenzionalna slika pretvara u \textit{niz} tokena. Na početak niza dodaje se
poseban, naučivi klasifikacioni token \texttt{[CLS]}, a svakom tokenu se pridodaje i
\textbf{pozicioni embeding} (engl.\ \textit{positional embedding}), pošto sam mehanizam
pažnje ne sadrži informaciju o redosledu.

Niz tokena prolazi kroz \textbf{enkoder transformera} --- stek identičnih slojeva od kojih
svaki sadrži višeglavu samopažnju (engl.\ \textit{multi-head self-attention}, MHSA) i
potpuno povezanu mrežu (MLP), uz normalizaciju slojeva (engl.\ \textit{layer norm}) i
rezidualne veze. Srž mehanizma je \textbf{samopažnja}: iz svakog tokena izvode se vektori
upita $Q$ (engl.\ \textit{query}), ključa $K$ (engl.\ \textit{key}) i vrednosti $V$
(engl.\ \textit{value}), a izlaz se računa kao
\begin{equation}
\mathrm{Attention}(Q,K,V) = \mathrm{softmax}\!\left(\frac{QK^{\top}}{\sqrt{d_k}}\right)V,
\label{eq:attention}
\end{equation}
gde je $d_k$ dimenzija ključeva. Na taj način svaki token može da ,,obrati pažnju'' na sve
ostale tokene i tako modeluje \textbf{globalne} zavisnosti između udaljenih delova slike
već u prvom sloju --- za razliku od konvolucija, koje su inherentno lokalne. Više
paralelnih ,,glava'' pažnje uči različite tipove odnosa. Konačna klasifikacija se izvodi iz
reprezentacije \texttt{[CLS]} tokena na izlazu enkodera, propuštanjem kroz mali MLP.
Varijanta \textbf{ViT-Base/16} (isečci $16\times16$) ima 12 slojeva enkodera, skrivenu
dimenziju 768, 12 glava pažnje i oko 86 miliona parametara.

Pošto transformeri nemaju ugrađenu induktivnu pristrasnost konvolucija (lokalnost i
translatornu ekvivarijantnost), oni po pravilu \textit{slabo generalizuju} kada se treniraju
od nule na malim skupovima podataka i zahtevaju \textbf{predtreniranje} na velikim skupovima.
Zbog toga se u ovom radu ViT-Base/16 ne trenira od nule, već se polazi od težina
predtreniranih na skupu ImageNet-21k i potom fino podešava (engl.\ \textit{fine-tuning}) na
našem zadatku.

\paragraph{Augmentacija podataka.}
Radi smanjenja preprilagođavanja (engl.\ \textit{overfitting}) na trening skup, tokom
treniranja se primenjuje augmentacija: slučajno isecanje sa promenom razmere (engl.\
\textit{random resized crop}), horizontalno preslikavanje, rotacija u malom opsegu i
blaga promena osvetljenja i kontrasta (engl.\ \textit{color jitter}). Na validacioni i
test skup ne primenjuje se augmentacija, već samo deterministička transformacija.

\paragraph{Funkcija gubitka i optimizacija.}
Za klasifikaciju u $C$ klasa koristi se unakrsna entropija (engl.\ \textit{cross-entropy})
\begin{equation}
\mathcal{L} = -\sum_{c=1}^{C} w_c \, y_c \log \hat{p}_c,
\label{eq:ce}
\end{equation}
gde je $y_c$ indikator tačne klase, $\hat{p}_c$ verovatnoća koju model dodeljuje klasi
$c$ (dobijena \textit{softmax} funkcijom nad izlaznim logitima), a $w_c$ težinski faktor
klase koji kompenzuje neuravnoteženost skupa podataka i računa se kao odnos veličine
najveće klase i veličine klase $c$. Optimizacija parametara vrši se algoritmom
Adam~\cite{kingma2015adam} (za ResNet-50), odnosno njegovom varijantom sa razdvojenim
opadanjem težina AdamW~\cite{loshchilov2019adamw} (za ViT, što je standardan izbor za
fino podešavanje transformera), uz kosinusno opadanje stope učenja (engl.\ \textit{cosine
annealing})~\cite{loshchilov2017sgdr}. Radi regularizacije koriste se \textit{dropout}
sloj~\cite{srivastava2014dropout} i $L_2$ regularizacija težina (engl.\ \textit{weight
decay}).

\paragraph{Grad-CAM.}
Grad-CAM~\cite{selvaraju2017gradcam} je metoda vizuelnog objašnjenja odluka CNN-a. Za
ciljnu klasu $c$, gradijent skora $y^c$ propagira se unazad do izabranog konvolucionog
sloja (poslednji blok, \texttt{layer4}). Globalnim usrednjavanjem gradijenata po
prostornim dimenzijama dobijaju se težine
\begin{equation}
\alpha_k^c = \frac{1}{Z}\sum_{i}\sum_{j}\frac{\partial y^c}{\partial A^k_{ij}},
\label{eq:gradcam-alpha}
\end{equation}
a toplotna mapa značajnosti se računa kao
\begin{equation}
L^c_{\text{Grad-CAM}} = \mathrm{ReLU}\!\left(\sum_k \alpha_k^c A^k\right),
\label{eq:gradcam}
\end{equation}
gde je $A^k$ $k$-ta mapa obeležja ciljnog sloja. Dobijena mapa se zatim normalizuje u
opseg $[0,1]$ i naložna prikazuje preko ulaznog snimka.

\subsection{Pregled algoritma}
\label{sec:pipeline}

Ceo sistem je prikazan blok-dijagramom na slici~\ref{fig:blokdijagram}. Sastoji se iz
četiri glavna bloka: (1) priprema podataka (sa augmentacijom), (2) klasifikator ---
jedna od dve arhitekture, ResNet-50 ili ViT-Base/16, (3) treniranje i (4) evaluacija sa
Grad-CAM vizuelizacijom (primenljivom samo na konvolucioni model). Pojedinačni blokovi
su detaljno opisani u nastavku.

\begin{figure}[H]
\centering
\begin{tikzpicture}[
    node distance=0.9cm and 1.1cm,
    blok/.style={rectangle,rounded corners,draw,thick,minimum width=3.0cm,
                 minimum height=1.0cm,align=center,fill=blokplava,font=\small},
    blokz/.style={blok,fill=blokzelena},
    blokn/.style={blok,fill=bloknarandzasta},
    strelica/.style={-{Stealth[length=2.5mm]},thick}
]
\node[blok] (data) {Priprema podataka\\\scriptsize (učitavanje, deljenje,\\\scriptsize augmentacija)};
\node[blok,right=of data] (pre) {Pretprocesiranje\\\scriptsize resize $224\!\times\!224$,\\\scriptsize normalizacija};
\node[blokz,right=of pre] (cnn) {Klasifikator\\\scriptsize ResNet-50 (od nule)\\\scriptsize \emph{ili} ViT-B/16 (pretr.)};
\node[blokz,below=of cnn] (head) {Glava + softmax\\\scriptsize (2 klase)};
\node[blokn,below=of pre] (train) {Treniranje\\\scriptsize Adam/AdamW, CE,\\\scriptsize cosine LR};
\node[blokn,below=of data] (eval) {Evaluacija\\\scriptsize tačnost, matrica\\\scriptsize konfuzije};
\node[blok,below=of eval,fill=white] (cam) {Grad-CAM\\\scriptsize toplotne mape\\\scriptsize (samo ResNet)};

\draw[strelica] (data) -- (pre);
\draw[strelica] (pre) -- (cnn);
\draw[strelica] (cnn) -- (head);
\draw[strelica] (head) -- (train);
\draw[strelica] (train) -- (eval);
\draw[strelica] (eval) -- (cam);
\end{tikzpicture}
\caption{Blok-dijagram celokupnog sistema za klasifikaciju MRI preseka lumbalne kičme.}
\label{fig:blokdijagram}
\end{figure}

\subsection{Baza podataka}
\label{sec:baza}

Korišćen je javno dostupan skup podataka \textit{Lumbar Spinal MRI
Dataset}~\cite{khan2023dataset}, dostupan na platformi Kaggle na adresi:\\
\url{https://www.kaggle.com/datasets/abdullahkhan70/lumbar-spinal-mri-dataset}.

\paragraph{Sadržaj i format.}
Skup se sastoji od \textbf{aksijalnih (transverzalnih) preseka} MRI snimaka lumbalne
kičme, tj.\ preseka u ravni okomitoj na osu kičme, na nivou intervertebralnih diskova
donjeg dela lumbalne kičme. Aksijalna ravan je za ovaj zadatak posebno informativna jer se
na njoj jasno vide intervertebralni disk, duralna (tekalna) vreća i nervni koreni, kao i
njihov međusobni odnos~\cite{hou2023mri}. Svaka slika je u \texttt{JPEG} formatu,
rezolucije $400\times400$ piksela, zapisana kao trokanalna (RGB) slika iako je sam MRI
sadržaj u nijansama sive. Slike su već anonimizovane i isečene na region od interesa, bez
pratećih meta-podataka o pacijentu.

\paragraph{Klase.}
Snimci su organizovani u tri originalne kategorije, prema dominantnom nalazu na preseku:
\begin{itemize}
  \item \textbf{Herniated Disc} (hernija diska) --- presek na kome materijal
        intervertebralnog diska probija fibrozni prsten i prodire u spinalni kanal,
        potencijalno pritiskajući nervne strukture; to je patološki nalaz koji se dovodi u
        vezu sa bolom u leđima i nozi.
  \item \textbf{No Stenosis} (uredan nalaz) --- presek bez suženja spinalnog kanala i bez
        znakova hernije; služi kao ,,negativna'' (zdrava) klasa.
  \item \textbf{Thecal Sac} (tekalna/duralna vreća) --- presek na kome je u fokusu duralna
        vreća (membranska struktura ispunjena likvorom koja obavija nervne korene). Ova
        kategorija ne predstavlja dijagnozu uporedivu sa preostale dve, već pre tip prikaza
        anatomske strukture.
\end{itemize}
U ovom radu klasa \textit{Thecal Sac} je \textbf{izostavljena} jer nije dijagnostički
uporediva sa preostale dve i unela bi nejasnu, heterogenu kategoriju u problem; time je
zadatak sveden na \textbf{binarnu klasifikaciju} (hernija diska naspram urednog nalaza),
što je u skladu i sa formulacijom problema u literaturi~\cite{hou2023mri}. Podaci su od
strane autora skupa unapred podeljeni u \texttt{train} i \texttt{test} foldere; raspodela
broja snimaka po klasama (nakon izostavljanja \textit{Thecal Sac}) data je u
tabeli~\ref{tab:baza}.

\begin{table}[H]
\centering
\caption{Broj snimaka po klasama u korišćenom skupu podataka (nakon izostavljanja klase
\textit{Thecal Sac}).}
\label{tab:baza}
\begin{tabular}{lccc}
\toprule
\textbf{Klasa} & \textbf{train folder} & \textbf{test folder} & \textbf{Ukupno} \\
\midrule
Hernija diska (Herniated Disc) & 3063 & 1218 & 4281 \\
Uredan nalaz (No Stenosis)     & 3152 & 1507 & 4659 \\
\midrule
\textbf{Ukupno}                & 6215 & 2725 & 8940 \\
\bottomrule
\end{tabular}
\end{table}

Validacioni skup formiran je objedinjavanjem $15\%$ stratifikovano izdvojenih snimaka iz
\texttt{train} foldera i snimaka iz \texttt{test} foldera, dok je preostalih $85\%$
\texttt{train} foldera korišćeno za treniranje. Primeri snimaka iz obe klase prikazani su
na slici~\ref{fig:primeri}.

\paragraph{Napomena o prikupljanju podataka.}
U ovom projektu nisu vršena sopstvena snimanja niti merenja na ispitanicima --- korišćen
je isključivo javno dostupan, već prikupljen i anonimizovan skup podataka. Stoga se delovi
metodologije koji se odnose na opis sistema za snimanje, sprovođenje eksperimenta sa
ispitanicima i prikupljanje saglasnosti (kao i prateći prilozi) ne primenjuju na ovaj rad;
za pitanja prikupljanja i etičke saglasnosti merodavan je izvorni autor skupa
podataka~\cite{khan2023dataset}.

\begin{figure}[H]
\centering
\placeholderbox{5cm}{Reprezentativni aksijalni MRI preseci: levo --- hernija diska,
desno --- uredan nalaz. (Slika se generiše skriptom opisanom u prilogu; videti
uputstvo za reprodukciju.)}
\caption{Reprezentativni primeri ulaznih snimaka iz dve klase.}
\label{fig:primeri}
\end{figure}

\subsection{Pretprocesiranje}
\label{sec:preproc}

Svaki snimak se skalira na rezoluciju $224\times224$ piksela i konvertuje u tenzor, nakon
čega se normalizuje po kanalima korišćenjem standardnih ImageNet~\cite{deng2009imagenet}
statistika (srednje vrednosti $[0{,}485;\,0{,}456;\,0{,}406]$ i standardne devijacije
$[0{,}229;\,0{,}224;\,0{,}225]$). Na \textbf{trening} skup dodatno se primenjuje
augmentacija: slučajno isecanje sa promenom razmere (\textit{random resized crop} sa
faktorom $0{,}8$--$1{,}0$), horizontalno preslikavanje (verovatnoća $0{,}5$), rotacija u
opsegu $\pm10^\circ$ i blaga promena osvetljenja i kontrasta (\textit{color jitter} sa
intenzitetom $0{,}2$). Na \textbf{validacioni} i \textbf{test} skup primenjuje se samo
deterministička transformacija (skaliranje i normalizacija), bez augmentacije. Zbog blage
neuravnoteženosti klasa, u funkciji gubitka koriste se težine klasa (videti
jednačinu~\ref{eq:ce}).

\subsection{Arhitekture klasifikatora}
\label{sec:arh}

Upoređene su dve arhitekture, predstavnici dveju različitih familija modela za
klasifikaciju slika.

\paragraph{ResNet-50 (konvolucioni model).}
Kao osnova (\textit{backbone}) koristi se ResNet-50~\cite{he2016deep}. Originalni
klasifikacioni sloj zamenjen je sekvencom koja se sastoji od \textit{dropout} sloja
(verovatnoća $0{,}5$) i potpuno povezanog (\textit{fully-connected}) sloja sa dva izlaza,
po jedan za svaku klasu. Ovaj model se trenira \textit{od nule} (bez ImageNet
predtreniranih težina), čime se ispituje sposobnost arhitekture da nauči obeležja
specifična za MRI domen direktno iz dostupnih podataka.

\paragraph{ViT-Base/16 (transformer model).}
Drugi model je vizuelni transformer ViT-Base/16~\cite{dosovitskiy2021vit}, učitan iz
biblioteke \texttt{timm}~\cite{wightman2019timm}. Za razliku od ResNet-a, ViT se
\textit{ne} trenira od nule već se fino podešava (\textit{fine-tuning}) polazeći od težina
predtreniranih na velikom skupu ImageNet-21k. Razlog je što transformeri, usled
nedostatka konvolucione induktivne pristrasnosti, slabo generalizuju kada se treniraju od
nule na svega nekoliko hiljada slika; predtreniranje je stoga praktično neophodno. Glava
za klasifikaciju (nad \texttt{[CLS]} tokenom) prilagođena je broju klasa (dve).

\subsection{Treniranje}
\label{sec:trening}

Treniranje je izvedeno na iznajmljenom grafičkom procesoru \textbf{NVIDIA RTX 4090
(24\,GB)} preko platforme \textit{vast.ai}. Obe arhitekture treniraju se istom skriptom,
u kojoj se izborom parametra \texttt{MODEL\_TYPE} (\texttt{"resnet50"} ili \texttt{"vit"})
automatski biraju odgovarajući model i hiperparametri. Korišćeni hiperparametri za oba
modela sumirani su u tabeli~\ref{tab:hiperparametri}. Optimizacija se vrši algoritmom
Adam~\cite{kingma2015adam} za ResNet-50, odnosno AdamW~\cite{loshchilov2019adamw} za ViT,
uz kosinusno opadanje stope učenja~\cite{loshchilov2017sgdr} tokom svih epoha. Radi
stabilnosti treniranja primenjeno je odsecanje norme gradijenta (engl.\ \textit{gradient
clipping}) na vrednost $1{,}0$, a tokom treniranja praćena je i prosečna norma gradijenta
po epohi.

Treniranje se odvija do unapred zadatog budžeta epoha, pri čemu se \textbf{čuva model sa
najboljom tačnošću na validacionom skupu} (što efektivno deluje kao rano zaustavljanje);
dodatno se na svakih $15$ minuta snima međučekpoint radi otpornosti na prekide. Tok
treniranja praćen je pomoću alata Weights \& Biases (\textit{wandb}); za konvolucioni model
se periodično (na svake dve epohe) u \textit{wandb} beleže i Grad-CAM vizuelizacije radi
praćenja na šta model obraća pažnju tokom učenja. Javno dostupni izveštaji o treniranju mogu
se pogledati na sledećim adresama:
\begin{itemize}
  \item ResNet-50: \url{[wandb-link-resnet]}
  \item ViT-Base/16: \url{[wandb-link-vit]}
\end{itemize}

\begin{table}[H]
\centering
\caption{Hiperparametri treniranja za obe arhitekture.}
\label{tab:hiperparametri}
\begin{tabular}{lll}
\toprule
\textbf{Hiperparametar} & \textbf{ResNet-50} & \textbf{ViT-Base/16} \\
\midrule
Inicijalizacija        & od nule                 & ImageNet-21k (pretreniran) \\
Ulazna rezolucija      & $224\times224$          & $224\times224$ \\
Broj epoha (budžet)    & do 2000$^{*}$           & do 2000$^{*}$ \\
Veličina batch-a       & 128                     & 128 \\
Optimizator            & Adam                    & AdamW \\
Početna stopa učenja   & $5\times10^{-4}$        & $1\times10^{-4}$ \\
Raspored stope učenja  & Cosine annealing        & Cosine annealing \\
Weight decay ($L_2$)   & $1\times10^{-3}$        & $1\times10^{-2}$ \\
Dropout (glava)        & $0{,}5$                 & ugrađen u arhitekturu \\
Augmentacija           & da                      & da \\
Funkcija gubitka       & Težinska CE             & Težinska CE \\
Gradient clipping      & $1{,}0$                 & $1{,}0$ \\
Seed                   & 42                      & 42 \\
\bottomrule
\end{tabular}

\vspace{2pt}
{\footnotesize $^{*}$ Treniranje ide do zadatog budžeta epoha, ali se čuva model sa
najboljom validacionom tačnošću (efektivno rano zaustavljanje).}
\end{table}

\subsection{Evaluacija i metrike}
\label{sec:metrike}

Kvalitet modela meri se na test skupu pomoću tačnosti (engl.\ \textit{accuracy}),
senzitivnosti (engl.\ \textit{sensitivity}/\textit{recall}), specifičnosti (engl.\
\textit{specificity}) i površine ispod ROC krive (AUC), uz prikaz matrice konfuzije. Za
pozitivnu klasu uzima se \textit{hernija diska}. Metrike se računaju na sledeći način:
\begin{equation}
\text{Acc}=\frac{TP+TN}{TP+TN+FP+FN},\quad
\text{Sens}=\frac{TP}{TP+FN},\quad
\text{Spec}=\frac{TN}{TN+FP}.
\label{eq:metrike}
\end{equation}
Nakon evaluacije, na odabranim primerima primenjuje se Grad-CAM
(odeljak~\ref{sec:teorija}) radi vizuelne provere da li se model fokusira na anatomski
relevantne regione.

% ===========================================================================
% 3. REZULTATI SA DISKUSIJOM
% ===========================================================================
\section{Rezultati sa diskusijom}
\label{sec:rezultati}

\subsection{Tok treniranja}
\label{sec:krive}

Na slici~\ref{fig:krive} prikazane su krive treniranja modela ResNet-50: gubitak (levo) i
tačnost (sredina) za trening i validacioni skup, kao i prosečna norma gradijenta po epohi
(desno). Krive pokazuju očekivano opadanje gubitka i porast tačnosti tokom treniranja,
dok norma gradijenta ostaje stabilna zahvaljujući odsecanju gradijenta. Odgovarajuće
krive za model ViT prikazane su na slici~\ref{fig:krive-vit}; zahvaljujući predtreniranju,
ViT konvergira u manjem broju epoha.

\begin{figure}[H]
\centering
\includegraphics[width=\linewidth]{figures/learning_curves.png}
\caption{Krive treniranja modela \textbf{ResNet-50}: gubitak, tačnost i prosečna norma
gradijenta po epohi za trening i validacioni skup.}
\label{fig:krive}
\end{figure}

\begin{figure}[H]
\centering
\placeholderbox{4.5cm}{Krive treniranja modela ViT-Base/16 (gubitak, tačnost, norma
gradijenta). Generiše se pokretanjem \texttt{train.py} sa \texttt{MODEL\_TYPE="vit"}
(datoteka \texttt{best\_model\_vit\_curves.png}); videti uputstvo za reprodukciju.}
\caption{Krive treniranja modela \textbf{ViT-Base/16}.}
\label{fig:krive-vit}
\end{figure}

\subsection{Konačni rezultati}
\label{sec:finalni}

Sumarni rezultati na test skupu prikazani su u tabeli~\ref{tab:rezultati}, a matrica
konfuzije na slici~\ref{fig:konfuzija}. \textit{(Numeričke vrednosti su ostavljene kao
rezervisana mesta i popunjavaju se nakon evaluacije istreniranog modela.)}

\begin{table}[H]
\centering
\caption{Rezultati klasifikacije na test skupu za obe arhitekture. Pozitivna klasa:
hernija diska.}
\label{tab:rezultati}
\begin{tabular}{lcc}
\toprule
\textbf{Metrika} & \textbf{ResNet-50} & \textbf{ViT-Base/16} \\
\midrule
Tačnost (Accuracy)        & [XX{,}X\%] & [XX{,}X\%] \\
Senzitivnost (Sensitivity)& [XX{,}X\%] & [XX{,}X\%] \\
Specifičnost (Specificity)& [XX{,}X\%] & [XX{,}X\%] \\
Preciznost (Precision)    & [XX{,}X\%] & [XX{,}X\%] \\
F1 mera                   & [0{,}XX]   & [0{,}XX] \\
AUC                       & [0{,}XX]   & [0{,}XX] \\
\bottomrule
\end{tabular}
\end{table}

\begin{figure}[H]
\centering
\placeholderbox{6cm}{Matrica konfuzije (2$\times$2) na test skupu, sa apsolutnim brojem i
procentom za svako polje. (Generiše se skriptom za evaluaciju; videti uputstvo za
reprodukciju.)}
\caption{Matrica konfuzije na test skupu (najbolji model).}
\label{fig:konfuzija}
\end{figure}

Radi detaljnijeg uvida, u tabeli~\ref{tab:poklasa} dat je sumarni prikaz rezultata
\textit{po klasama} (preciznost, odziv i F1 mera za svaku od dve klase), čime se vidi da
li model podjednako dobro prepoznaje obe kategorije ili favorizuje jednu.

\begin{table}[H]
\centering
\caption{Sumarni rezultati po klasama na test skupu (najbolji model).}
\label{tab:poklasa}
\begin{tabular}{lcccc}
\toprule
\textbf{Klasa} & \textbf{Preciznost} & \textbf{Odziv} & \textbf{F1} & \textbf{Br. uzoraka} \\
\midrule
Hernija diska (Herniated Disc) & [0{,}XX] & [0{,}XX] & [0{,}XX] & 1218 \\
Uredan nalaz (No Stenosis)     & [0{,}XX] & [0{,}XX] & [0{,}XX] & 1507 \\
\midrule
Prosek (\textit{macro})        & [0{,}XX] & [0{,}XX] & [0{,}XX] & 2725 \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Detaljan prikaz međukoraka na jednom primeru}
\label{sec:jedanprimer}

Da bi se ilustrovali međukoraci obrade, na jednom reprezentativnom uzorku (aksijalni
presek sa hernijom diska) prikazan je ceo put kroz sistem, korak po korak:
\begin{enumerate}
  \item \textbf{Ulazni snimak} se učitava i skalira na $224\times224$ piksela
        (slika~\ref{fig:jedanprimer}, levo).
  \item \textbf{Pretprocesiranje:} snimak se normalizuje ImageNet statistikama; na test
        skupu se ne primenjuje augmentacija.
  \item \textbf{Prolaz kroz mrežu:} model vraća logite, a \textit{softmax} ih pretvara u
        verovatnoće klasa --- u ovom primeru $P(\text{hernija})=[0{,}XX]$ i
        $P(\text{uredan})=[0{,}XX]$, te se uzorak klasifikuje kao
        \textit{hernija diska}.
  \item \textbf{Grad-CAM:} za predviđenu klasu računa se toplotna mapa značajnosti
        (slika~\ref{fig:jedanprimer}, sredina), koja se zatim superponira preko ulaznog
        snimka (slika~\ref{fig:jedanprimer}, desno); aktivacije se koncentrišu u regionu
        diska, što potvrđuje da je odluka doneta na osnovu anatomski relevantnog regiona.
\end{enumerate}

\begin{figure}[H]
\centering
\placeholderbox{4.5cm}{Detaljan prikaz na jednom primeru: ulazni snimak, Grad-CAM mapa i
superpozicija, sa ispisanim verovatnoćama klasa. (Pojedinačne slike već generiše
\texttt{gradcam\_viz.py}; videti uputstvo za reprodukciju.)}
\caption{Međukoraci obrade na jednom reprezentativnom primeru.}
\label{fig:jedanprimer}
\end{figure}

\subsection{Vizuelizacija pomoću Grad-CAM metode}
\label{sec:gradcam-rez}

Na slici~\ref{fig:gradcam} prikazane su Grad-CAM toplotne mape za reprezentativne uzorke
iz trening i test skupa. Za svaki uzorak prikazani su ulazni snimak (levo), toplotna mapa
značajnosti (sredina) i njihova superpozicija (desno), zajedno sa tačnom i predviđenom
klasom. Uočava se da se kod ispravno klasifikovanih primera aktivacije pretežno
koncentrišu u regionu intervertebralnog diska i duralne vreće, što je u skladu sa
anatomskim kriterijumima dijagnostike i sa zapažanjima iz literature~\cite{hou2023mri}.
Napomenimo da je Grad-CAM primenljiv samo na konvolucioni model (ResNet-50), pošto se
oslanja na prostorne mape obeležja konvolucionih slojeva. Vizuelni transformer takve mape
nema, te bi za njegovu interpretaciju bile potrebne metode zasnovane na pažnji (npr.\
\textit{attention rollout}), što ostavljamo za budući rad.

\begin{figure}[H]
\centering
\includegraphics[width=0.85\linewidth]{figures/gradcam_summary.png}
\caption{Grad-CAM vizuelizacije za reprezentativne uzorke (kolone: ulaz, toplotna mapa,
superpozicija). Iznad svakog reda navedeni su tačna i predviđena klasa.}
\label{fig:gradcam}
\end{figure}

\subsection{Analiza promašaja}
\label{sec:promasaji}

Na slici~\ref{fig:promasaj} prikazani su tipični primeri pogrešno klasifikovanih snimaka.
Greške se najčešće javljaju kod graničnih slučajeva blage protruzije diska, kao i kod
snimaka sa izraženim artefaktima ili netipičnom orijentacijom. Grad-CAM mape kod
promašaja često pokazuju rasute ili pomerene aktivacije van regiona diska, što ukazuje na
to da model u tim slučajevima nije izdvojio dijagnostički relevantno obeležje.

\begin{figure}[H]
\centering
\placeholderbox{5cm}{Primeri pogrešno klasifikovanih snimaka sa pripadajućim Grad-CAM
mapama i obrazloženjem (granični slučajevi, artefakti). Bira se iz izlaza skripte za
Grad-CAM gde je \texttt{pred} $\neq$ \texttt{true}.}
\caption{Primeri promašaja modela uz Grad-CAM objašnjenje.}
\label{fig:promasaj}
\end{figure}

\subsection{Poređenje sa literaturom}
\label{sec:poredjenje}

U tabeli~\ref{tab:poredjenje} naši rezultati za obe arhitekture upoređeni su sa
rezultatima Hou i saradnika~\cite{hou2023mri}. Treba imati u vidu da poređenje nije
potpuno direktno: oni koriste polu-nadgledano kontrastivno predtreniranje i višeslojni
ulaz (tri susedna aksijalna preseka), dok mi koristimo potpuno nadgledani pristup sa
jednim presekom. Uprkos tome, dobijene vrednosti su uporedive, što sugeriše da i direktni
klasifikatori mogu da postignu klinički interesantne rezultate na ovom zadatku.

\begin{table}[H]
\centering
\caption{Poređenje rezultata obe arhitekture sa rezultatima iz literature.}
\label{tab:poredjenje}
\begin{tabular}{lcccc}
\toprule
\textbf{Metod} & \textbf{Tačnost} & \textbf{Senzitivnost} & \textbf{Specifičnost} & \textbf{AUC} \\
\midrule
Hou i sar.~\cite{hou2023mri} (polu-nadgledano) & $87{,}11\%$ & $87{,}50\%$ & $86{,}72\%$ & $0{,}9487$ \\
Naš model --- ResNet-50 (od nule)              & [XX{,}X\%]  & [XX{,}X\%]  & [XX{,}X\%]  & [0{,}XX] \\
Naš model --- ViT-B/16 (pretreniran)           & [XX{,}X\%]  & [XX{,}X\%]  & [XX{,}X\%]  & [0{,}XX] \\
\bottomrule
\end{tabular}
\end{table}

\paragraph{Diskusija.}
Glavne sličnosti sa literaturom su izbor aksijalnih preseka kao ulaza i upotreba
toplotnih mapa za interpretaciju. Glavne razlike su u složenosti metode: naš pristup ne
zahteva fazu samonadgledanog predtreniranja na MRI podacima, ali je zato osetljiviji na
količinu i kvalitet označenih podataka. Korišćenje samo jednog preseka umesto tri susedna
potencijalno gubi deo informacije u vertikalnom pravcu, što može objasniti eventualne
razlike u performansama na graničnim slučajevima. Što se tiče poređenja dve arhitekture,
očekuje se da predtrenirani ViT bolje generalizuje na ovako malom skupu zahvaljujući
znanju prenetom sa ImageNet-21k, dok ResNet-50 treniran od nule mora celokupnu
reprezentaciju da nauči iz svega nekoliko hiljada snimaka; konačno poređenje sledi iz
popunjenih vrednosti u tabeli~\ref{tab:rezultati}.

% ===========================================================================
% 4. ZAKLJUČAK
% ===========================================================================
\section{Zaključak}
\label{sec:zakljucak}

U ovom radu predstavljen je sistem za binarnu klasifikaciju aksijalnih MRI preseka
lumbalne kičme u kategorije hernija diska i uredan nalaz, pri čemu su upoređene dve
arhitekture: konvoluciona mreža ResNet-50 trenirana od nule i vizuelni transformer
ViT-Base/16 fino podešen polazeći od ImageNet-21k predtreniranih težina. Pokazano je da
relativno jednostavni, potpuno nadgledani pristupi postižu rezultate uporedive sa
složenijom polu-nadgledanom metodom iz literature~\cite{hou2023mri}. Primenom Grad-CAM
metode na konvolucioni model potvrđeno je da se on pretežno fokusira na anatomski
relevantne regione, čime se povećava poverenje u njegove odluke i omogućava lakše
otkrivanje uzroka grešaka.

\paragraph{Smernice za budući rad.}
Kao pravci daljeg istraživanja izdvajaju se: (i) sistematsko poređenje konvolucionih i
transformer arhitektura uz unakrsnu validaciju i analizu uticaja predtreniranja;
(ii) interpretabilnost ViT modela pomoću metoda zasnovanih na pažnji (npr.\ \textit{attention
rollout}), pošto Grad-CAM nije primenljiv na transformere; (iii) prelazak sa jednog na
više susednih aksijalnih preseka, po uzoru na~\cite{hou2023mri}, radi uključivanja
vertikalne informacije; (iv) proširenje na više klasa (npr.\ stepenovanje težine stenoze)
i (v) kvantitativna evaluacija kvaliteta toplotnih mapa u saradnji sa radiologom.

\section{Dostupnost koda i podataka}
\label{sec:dostupnost}

Kompletan izvorni kôd projekta (skripte za pripremu podataka, treniranje obe
arhitekture, evaluaciju i Grad-CAM vizuelizaciju) javno je dostupan u GitHub
repozitorijumu na adresi:
\begin{center}
\url{[github-link]}
\end{center}
Skup podataka je javno dostupan na platformi Kaggle~\cite{khan2023dataset}
(videti odeljak~\ref{sec:baza}).

% ===========================================================================
% LITERATURA
% ===========================================================================
\newpage
\bibliographystyle{ieeetr}
\bibliography{references}

\end{document}
